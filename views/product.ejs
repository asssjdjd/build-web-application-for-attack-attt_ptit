<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Quản lý sản phẩm</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="container mt-5">
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/dashboard">Dashboard</a>
    <div class="collapse navbar-collapse">
      <ul class="navbar-nav">
        <li class="nav-item"><a class="nav-link" href="/dashboard/users">Quản lý User</a></li>
        <li class="nav-item"><a class="nav-link" href="/dashboard/products">Quản lý Quần Áo</a></li>
      </ul>
    </div>
    <a href="/logout" class="btn btn-outline-danger">Logout</a>
  </nav>

  <h3 class="mt-4">Thêm sản phẩm mới</h3>
  <form method="POST" class="mb-4">
  <div class="row">
    <div class="col-md-2"><input type="text" name="name" placeholder="Tên" class="form-control" required></div>
    <div class="col-md-2"><input type="number" name="price" placeholder="Giá" class="form-control" required></div>
    <div class="col-md-2"><input type="number" name="sold" placeholder="Sold" class="form-control" value="0"></div>
    <div class="col-md-2">
      <select name="size" class="form-control">
        <option>S</option><option>M</option><option>L</option><option>XL</option>
      </select>
    </div>
    <div class="col-md-2"><input type="text" name="description" placeholder="Mô tả" class="form-control"></div>
    <div class="col-md-2"><button type="submit" class="btn btn-success w-100">Thêm</button></div>
  </div>
</form>

<table class="table table-striped">
  <thead>
    <tr><th>Tên</th><th>Giá</th><th>Sold</th><th>Doanh Số</th><th>Kích cỡ</th><th>Mô tả</th></tr>
  </thead>
  <tbody>
    <% products.forEach(product => { %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.price %></td>
        <td><%= product.sold %></td>
        <td><strong><%= (product.revenue || 0).toLocaleString('vi-VN') %> ₫</strong></td>
        <td><%= product.size %></td>
        <td><%= product.description %></td>
      </tr>
    <% }) %>
  </tbody>
  </table>
</body>
</html>